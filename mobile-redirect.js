/*
 * Mobile redirect — RETIRED in V11.
 *
 * The site is now fully responsive (one premium experience for every
 * device), so we no longer route phones to the separate /mobile.html
 * page. A single responsive site is better for SEO, maintenance, and
 * brand consistency. /mobile.html is kept in the repo as a backup and
 * can still be reached directly.
 *
 * This file is intentionally a no-op so older pages that still
 * reference it do not 404.
 */
(function () { /* no redirect */ })();
