const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});
document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle?.setAttribute('aria-expanded','false')}));
const form=document.querySelector('#leadForm');form?.addEventListener('submit',e=>{const status=form.querySelector('.form-status');const fd=new FormData(form);const first=String(fd.get('first_name')||'').trim();const last=String(fd.get('last_name')||'').trim();const email=String(fd.get('email')||'').trim();if(!first||!last||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){e.preventDefault();status.textContent='Please enter your first name, last name, and a valid email.';status.style.color='#B42318';return;}const interest=String(fd.get('primary_interest_display')||'').trim();const newsletter=fd.get('newsletter_opt_in_display')?'Yes':'No';const sms=fd.get('sms_consent_display')?'Yes':'No';const desc=form.querySelector('textarea[name="description"]');if(desc){const base=desc.value.trim();desc.value=[base,interest&&`Primary interest: ${interest}`,`Newsletter opt-in: ${newsletter}`,`SMS consent: ${sms}`,'Source: Rae & Co Capital website contact form'].filter(Boolean).join('\n');}status.textContent='Submitting securely...';status.style.color='#1F6F43';});
